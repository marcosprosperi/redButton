import React, {useState} from "react"
import CanvasDraw from "react-canvas-draw"
import { SketchPicker  } from 'react-color'
/*
function ValueLabelComponent(props) {
    const { children, open, value } = props
  
    const popperRef = React.useRef(null)
    React.useEffect(() => {
      if (popperRef.current) {
        popperRef.current.update()
      }
    })
  
    
  }

const Drawer = () => {
    
    const [color, setColor] = useState("#ffc600")
    const [brushRadius, setBrushRadius] = useState(10)

    return (
        <div class="drawer-board">    
            
            <CanvasDraw hideGrid={true} brushColor={color} brushRadius={brushRadius} lazyRadius={1} canvasWidth={400} canvasHeight={400} />
            <SketchPicker  color={color} onChangeComplete={(x,y) => setColor(x.hex)} disableAlpha={true} />
        </div>
        
    )
}
*/
export default null
